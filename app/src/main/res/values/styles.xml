<resources>

    <!-- Base application theme. -->
    <style name="AppThemeBase" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="cardViewStyle">@style/card_style_white</item>
    </style>

    <style name="AppTheme" parent="AppThemeBase">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
    </style>

    <style name="SplashTheme" parent="AppThemeBase">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_splash_top_background</item>
        <item name="colorPrimaryDark">@color/color_splash_top_background</item>
        <item name="colorAccent">@android:color/white</item>
    </style>

    <style name="card_style_white" parent="CardView">
        <item name="cardBackgroundColor">@android:color/white</item>
        <item name="cardElevation">4dp</item>
        <item name="cardCornerRadius">4dp</item>
        <item name="cardPreventCornerOverlap">false</item>
    </style>

</resources>
